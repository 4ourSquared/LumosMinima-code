name: Test del Server
'on':
    push:
      branches:
        - main

jobs:
    test:
      runs-on: ubuntu-latest
      steps:
        - uses: kong/setup-inso@v1
          with:
            inso-version: 3.5.0
        - run: inso --version
  